---
rules:
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    these_tests_failed:
    - gritar tiene que usar el parámetro
  then: Hmm... `gritar` está devolviendo siempre lo mismo :thinking_face:; ¿estás
    usando el parámetro?
